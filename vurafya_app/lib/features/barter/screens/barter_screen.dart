import 'package:flutter/material.dart';

class BarterScreen extends StatelessWidget {
  const BarterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Barter Trade')),
      body: const Center(child: Text('Barter marketplace — coming soon')),
    );
  }
}
